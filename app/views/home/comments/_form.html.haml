.js_comment_form
  %div
    = image_tag current_user.gravatar_url(length: 30)

  = bootstrap_form_for comment, url: home_comments_path, method: :post, remote: true do |f|
    = f.hidden_field :article_id
    = f.text_area :body
    = f.submit '送信'
